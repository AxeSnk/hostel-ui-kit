mixin form(type, name, placeholder)
    form
        input(class!=attributes.class type=type, name=name, placeholder=placeholder)

.field_default
    h3 text field_default
    +form('email','email','Еmail')