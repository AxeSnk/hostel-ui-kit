mixin menu({ list=[] })
  .menu
    nav.nav-menu
      each item in list
        .nav-menu__item(class= item.bold)= item.title
          if item.more
            i.material-icons.expand-more__icon expand_more
